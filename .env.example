# Comma-separated hostnames that are allowed to serve the Homepage UI.
# Replace dashboard.example.internal with the DNS name or LAN address you use.
HOMEPAGE_ALLOWED_HOSTS=localhost:3000,127.0.0.1:3000,dashboard.example.internal:3000
